language: python
sudo: false
env:
    - TOXENV=py27
    - TOXENV=py35
    - TOXENV=py36
before_install:
  - sudo make install-office
install:
    - travis_retry pip install tox
script:
    - tox
notifications:
    email: false
after_success:
  - coveralls
